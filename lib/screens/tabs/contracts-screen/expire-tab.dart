import 'package:flutter/material.dart';

class ExpireContractsTab extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text(
        'Expire Contracts',
        style: TextStyle(fontSize: 30),
      ),
    );
  }
}
